# MuseTag 产品规范

## 核心任务

唯一主路径是：**拖入图片 → AI 看图 → 从受控标签库推荐 5～8 个标签 → 用户删除或补选 → 确认完成**。

AI 建议与正式标签必须是两个状态。没有确认时，素材显示“待确认”；只有用户确认后，建议才写入正式标签并用于检索。

## 第一版范围

| 能力 | 要求 |
| --- | --- |
| 导入 | 拖放、文件选择、多张导入；JPG、PNG、WebP |
| AI 标签 | 导入后自动分析；每张推荐 5～8 个；仅从受控词表选择 |
| 人工确认 | 可删除建议、从规范词库补选、确认完成；不默认自由造词 |
| 保存 | 原图、正式标签、建议状态、备注、来源链接 |
| 查找 | 缩略图；文件名、标签、备注搜索；多标签 AND 筛选 |
| 使用 | 大图预览、修改标签、导出原图 |
| 安全 | 密钥仅在服务端；删除确认；完整备份与恢复 |

第一版不做登录、云同步、团队协作、视频解析、网页自动抓取或手机 App。

## 标签规则

- 词表定义在 `assets/starter/src/taxonomy.json`，详见 [tag-taxonomy.md](tag-taxonomy.md)。
- AI 不能输出词表之外的词；程序还要二次过滤模型结果。
- 同义概念只保留一个首选词，避免“封面／封面图／封面素材”并存。
- AI 建议优先覆盖主体、媒介、场景、构图、色彩、光线、风格；用途只有明显时才选。
- 每张 AI 建议 5～8 个，用户确认时可以删到更少；手动补选后最多 8 个。
- 词表升级需要版本号和迁移说明，不能静默改名导致旧素材失联。

## 默认界面

电脑端单页布局：顶部为搜索、导入和备份；左侧为素材范围与正式标签筛选；中央为缩略图和 AI 状态；右侧为图片、AI 建议与确认操作。

视觉基调为米白、卡其、灰绿与深棕。排版克制、边框轻、留白充分。状态必须一眼可分辨：AI 分析中、待确认、识别失败、已完成。

## 本地存储与 AI 边界

原图和元数据默认保存在 IndexedDB。识别时图片会发送到用户配置的 OpenAI API；界面和说明书必须明确这一点。API Key 仅由 Vite 本地服务读取，绝不能进入前端包、IndexedDB 或备份。

请求使用 `store: false`。这不等于图片从未离开电脑，也不应被表述为绝对零保留；隐私说明参照 OpenAI 官方数据控制文档。

## 失败与恢复

- 没有 API Key、网络错误、格式错误或模型拒绝时，素材仍安全保存在本地。
- 失败状态提供“重新识别”，同时允许从规范词库手动选择并保存。
- 刷新后，排队中或中断的分析可重新启动。
- 完整备份包含原图、正式标签、AI 状态、建议标签、备注和来源链接。
