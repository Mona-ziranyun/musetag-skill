# MuseTag 使用说明书

## 1. 第一次启动

1. 使用仓库脚本生成项目，或直接进入 `skills/musetag/assets/starter`。
2. 把 `.env.example` 复制为 `.env`。
3. 在 `.env` 中填写 `OPENAI_API_KEY`；不要把这个文件上传到 GitHub。
4. 运行 `npm install` 和 `npm run dev`，打开终端显示的本地地址。

## 2. 整理一张图片

把 JPG、PNG 或 WebP 拖进虚线框。MuseTag 会先把原图保存到当前浏览器，再请求 AI 分析。

AI 完成后，图片卡片显示“待确认”，右侧给出 5～8 个标签。点击某个标签即可删除；如果缺少重要信息，可在下方按分组补选。每张素材最多 8 个标签。

点击“确认标签并完成”后，这些词才成为正式标签并参与搜索。

## 3. 为什么不能随便新建标签

MuseTag 的目标不是描述得越多越好，而是让你以后稳定找回素材。自由输入很容易形成“封面／封面图／封面素材”这样的近义重复，因此默认只允许使用规范词库。

词库按用途、媒介、主体、场景、构图、版式、色彩、光线、风格和氛围组织。要修改词库，请编辑 `src/taxonomy.json`，并同步更新版本号和迁移说明。

## 4. 找回素材

- 顶部搜索会匹配文件名、正式标签和备注。
- 左侧可同时选择多个标签；结果必须同时包含所有选中标签。
- 点击素材可查看大图、调整标签、记录备注和来源链接。
- “导出原图”会下载浏览器中保存的原文件格式。

## 5. AI 失败怎么办

没有 API Key、网络中断或服务异常时，图片仍已保存在本地。点击“重新识别”，或直接从规范词库手动选择并保存。

## 6. 备份与恢复

点击“导出备份”会生成一个 JSON 文件，其中包含原图、标签、备注、来源和 AI 状态。恢复时选择这个文件；相同 ID 的素材会被备份内容覆盖。

浏览器数据可能被清理，所以来源原图和定期备份都应保留。

## 7. 隐私边界

素材库本体保存在浏览器 IndexedDB。AI 分析时，当前图片会发送到你在 `.env` 中配置的 OpenAI API。API Key 只由本地服务读取，不会写入浏览器或备份。请求设置 `store: false`，但这不应理解为图片从未离开电脑；具体处理规则以 OpenAI 官方数据控制文档为准。
